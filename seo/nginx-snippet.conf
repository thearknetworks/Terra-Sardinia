# Snapshot HTML for multilingual blog (mirrors pathname: /en/blog/post-slug → prerender/en/blog/post-slug/index.html)
# Build root (CRA): adjust to your deploy path
# map $request_uri $snapshot_path {
#   default "";
# }
# Use try_files with locations per your layout; example for lang-prefixed blog:
#
# location ~ ^/([a-z]{2})/blog/([^/]+)/?$ {
#   root /var/www/terra/prerender;
#   try_files /$1/blog/$2/index.html @spa;
# }
#
# location ~ ^/([a-z]{2})/blog/?$ {
#   root /var/www/terra/prerender;
#   try_files /$1/blog/index.html @spa;
# }
#
# location @spa {
#   root /var/www/terra/app/build;
#   try_files $uri /index.html;
# }
